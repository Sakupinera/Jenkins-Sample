pipeline {
    agent any

    environment {
        GIT_REPO = 'https://github.com/Sakupinera/.NET-Sample.git'
        GIT_BRANCH = 'main'
        TEST_SERVER = '192.168.118.98'
        DEPLOY_SERVER = '192.168.118.98'
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out source code...'
                git branch: "${GIT_BRANCH}", url: "${GIT_REPO}"
            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'dotnet build ./DotnetSample/DotnetSample.sln'
            }
        }
        // stage('Test') {
        //     steps {
        //         echo 'Testing...'
        //         sh """
        //         ssh sakuya@${TEST_SERVER} 'cd /path/to/your/project && dotnet test ./DotnetSample/DotnetSample.sln'
        //         """
        //     }
        // }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                sh """
                ssh sakuya@${DEPLOY_SERVER} 'mkdir -p /var/www/dotnet-sample'
                scp -r ./DotnetSample/bin/Release/net8.0/publish/* sakuya@${DEPLOY_SERVER}:/var/www/dotnet-sample/
                ssh sakuya@${DEPLOY_SERVER} 'cd /var/www/dotnet-sample && dotnet DotnetSample.dll'
                """
            }
        }
    }
}
